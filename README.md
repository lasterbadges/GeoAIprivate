# GeoAIprivate

python3 prepare_dataset.py --input_dir raw_images --output_dir dataset --val_ratio 0.2 - скрипт(вставить в консоль) для авторазметки маски

python3 seed_fill_correction.py --image raw_images/ore.JPG --output dataset/train/masks/ore.png - что бы открыть ручное выделениме маски(если скрипт умрёт)

